// //this is how we will use our connection string to connect to our database
// import { MongoClient } from "mongodb";
// import dotenv from "dotenv";
// dotenv.config();

// //

// //process.env.ATLAS_URI is speaks directly to ATLAS_URI in the .env file and gets the string which the string connects to the database
// //
// const client = new MongoClient(process.env.ATLAS_URI);

// //creating a new connection
// let conn;
// try {
//     //using the client = string and then using connect is how we get information to and from the database
//   conn = await client.connect();
// } catch (e) {
//   console.error(e);
// }

// //here is where we can download specific database and then we export it below so we can use it
// //sample_training is one of the sample databases 
// //we built everything to get to this line of code to export it so we can use it through the rest of the app
// let db = conn.db("sample_training");

// //this export will be imported into /routes/grades.mjs
// export default db;